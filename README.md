# Cortical-cell-counter
This MATLAB function lets you pick multiple cortical column images, define top/bottom bounds, and click cells in selected color channels. It converts each click to a normalized depth (0–1), plots scatter/density (and layer pies if defined), and exports results to Excel.
